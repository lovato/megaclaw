FROM openclaw-base:latest

ENV DEBIAN_FRONTEND=noninteractive

# Onboarding config is baked into this image via build-runtime.sh (podman commit)
# Run: task build:runtime  to build and onboard interactively

CMD ["openclaw", "gateway"]
