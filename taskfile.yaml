version: '3'

tasks:
  build:
    desc: Build the OpenClaw container image
    cmds:
      - ./scripts/build.sh

  run:
    desc: Run the OpenClaw container
    cmds:
      - ./scripts/run.sh

  onboard:
    desc: Run onboarding for first-time setup
    cmds:
      - podman run -it -v ./db:/home/node/.openclaw openclaw-local openclaw onboard
